---

- name: verify DPDK host preparation
  hosts: ocudu_nodes
  gather_facts: false

  vars_files:
    - "vars/main.yml"

  environment: "{{ proxy_env }}"

  tasks:
    - name: verify DPDK through role task
      ansible.builtin.include_role:
        name: dpdk
        tasks_from: verify
