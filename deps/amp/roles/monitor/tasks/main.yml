---

- name: verify helm prerequisites for amp/monitor
  import_role:
    name: preflight
    tasks_from: helm.yml

- name: verify kubeconfig prerequisites for amp/monitor
  import_role:
    name: preflight
    tasks_from: kubeconfig.yml
  vars:
    kubeconfig_component_name: AMP monitor
    kubeconfig_help_text: >-
      Run the k8s provisioning flow first, or set kubeconfig_source and rerun
      the install path that bootstraps it.

- name: install amp/monitor
  import_tasks: roles/monitor/tasks/install.yml
  tags: install

- name: uninstall amp/monitor
  import_tasks: roles/monitor/tasks/uninstall.yml
  tags: uninstall
